// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Compressed profile image for quicker loading."

// Push to remote repo
git push -u origin master --force

// Pull changes
git pull
