// Add all to local repo
git add .

// Commit local repo with message
git commit -m "Added Partial Mobile Formatting"

// Push to remote repo
git push -u origin master --force

// Pull changes
git pull
